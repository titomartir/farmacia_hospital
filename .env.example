# ==================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==================================
DB_HOST=db
DB_PORT=5432
DB_NAME=farmacia_dinamica
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION

# ==================================
# CONFIGURACIÓN DEL BACKEND
# ==================================
NODE_ENV=development
BACKEND_PORT=3000

# JWT Configuration
JWT_SECRET=CHANGE_ME_IN_PRODUCTION_use_random_string
JWT_EXPIRES_IN=24h

# CORS Origin (separar múltiples con comas)
CORS_ORIGIN=http://localhost:5173

# ==================================
# CONFIGURACIÓN DEL FRONTEND
# ==================================
FRONTEND_PORT=5173

# IMPORTANTE: Configurar según el escenario de acceso
# =====================================================
# ESCENARIO 1 - Solo acceso local (navegador en el mismo servidor):
#   VITE_API_URL=http://localhost:3000/api
#
# ESCENARIO 2 - Acceso desde red local (otros equipos):
#   VITE_API_URL=http://IP_DEL_SERVIDOR:3000/api
#   Ejemplo: VITE_API_URL=http://192.168.1.100:3000/api
#
# ESCENARIO 3 - Acceso mixto (recomendado para servidores):
#   Usa la IP del servidor para que funcione tanto local como desde red
#   VITE_API_URL=http://IP_DEL_SERVIDOR:3000/api
#
# Nota: Después de cambiar esta variable, ejecuta:
#   docker-compose down && docker-compose up -d
VITE_API_URL=http://localhost:3000/api

# ==================================
# CONFIGURACIÓN DE PGADMIN
# ==================================
PGADMIN_EMAIL=admin@farmacia.com
PGADMIN_PASSWORD=admin123

# ==================================
# NOTAS IMPORTANTES
# ==================================
# 1. Copiar este archivo como .env y cambiar los valores
# 2. NUNCA commitear el archivo .env al repositorio
# 3. En producción, usar contraseñas seguras y aleatorias
# 4. Si cambias VITE_API_URL, debes reiniciar los contenedores
